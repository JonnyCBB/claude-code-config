# Jira integration

When working with Jira via the Atlassian MCP:

- Use standard **Markdown** formatting — not Jira wiki markup.
- The Atlassian MCP tool strips hyperlink URLs from rich text. Always include raw URLs inline rather than relying on hyperlink formatting.
